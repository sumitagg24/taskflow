// Re-export all components for easier imports
// NOTE: Individual component files are imported directly throughout the app.
// This barrel file is kept for potential future use.

// Dead exports removed (TS errors, no consumers):
//   EmailNotificationModal — file doesn't exist
//   Navbar, Sidebar — default exports misnamed as named exports
